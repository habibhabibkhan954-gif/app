name: BUILD APK NEW Automatic

on:
  push:                # no branches filter — runs on push to any branch
  pull_request:        # runs on PRs targeting any branch
  workflow_dispatch:   # manual trigger

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install Dependencies
        run: bun install

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Make Gradle wrapper executable
        run: chmod +x gradlew
        working-directory: android

      - name: Build (assembleRelease) using wrapper
        run: ./gradlew assembleRelease -Pandroid.useAndroidX=true -Pandroid.enableJetifier=true -PhermesEnabled=true -PnewArchEnabled=false
        working-directory: android
        env:
          SAUSICO_RELEASE_STORE_FILE: ${{ github.workspace }}/release.keystore
          SAUSICO_RELEASE_STORE_PASSWORD: "opensource_build_key"
          SAUSICO_RELEASE_KEY_ALIAS: "release-alias"
          SAUSICO_RELEASE_KEY_PASSWORD: "opensource_build_key"
          GRADLE_OPTS: -Dorg.gradle.jvmargs="-Xmx4096m -XX:MaxMetaspaceSize=1024m"

      - name: Upload Release APK as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-apk-${{ github.run_number }}
          path: android/app/build/outputs/apk/release/*.apk
